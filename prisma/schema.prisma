generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Output {
  id        Int      @id @default(autoincrement())
  htmlCode  String
  createdAt DateTime @default(now())
}
